server:
  port: 8080
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:h2:mem:okuuim

#todo h2 replace with another database
#todo make other microservice for database
  h2:
    console:
      path: /h2-console
      settings:
        web-allow-others: true

  liquibase:
    enabled: true