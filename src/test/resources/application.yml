spring:
  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:h2:mem:test_testdb

  liquibase:
    enabled: true
    change-log: changelog/db.changelog-master.yaml
    contexts: test